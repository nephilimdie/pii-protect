# Lightweight dependencies for CI unit tests.
# Excludes ML-heavy packages: torch, spacy, transformers, onnxruntime,
# presidio-analyzer, presidio-anonymizer (all mocked in unit tests).

fastapi==0.137.2
httpx==0.27.2
pydantic-settings==2.5.2
cryptography==43.0.1
passlib[bcrypt]==1.7.4
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
alembic==1.13.3
faker==26.0.0
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
